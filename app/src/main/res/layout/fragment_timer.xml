<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/timerRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/timer_bg_idle"
    android:clickable="true"
    android:focusable="true">

    <TextView
        android:id="@+id/timeText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="@string/timer_initial"
        android:textColor="@color/timer_text"
        android:textSize="72sp"
        android:textStyle="bold"
        android:fontFeatureSettings="tnum" />

    <TextView
        android:id="@+id/hintText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal|bottom"
        android:layout_marginBottom="64dp"
        android:text="@string/timer_idle"
        android:textColor="@color/timer_text"
        android:textSize="18sp"
        android:alpha="0.7" />

    <!-- Subtle dismiss: only shown while arming/running, in the top-right corner.
         Lets a cuber bail out of a bad attempt (a mis-tap or interruption) by
         stopping the timer and discarding the time instead of recording it. -->
    <ImageButton
        android:id="@+id/dismissButton"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="top|end"
        android:layout_margin="8dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/timer_dismiss"
        android:src="@drawable/ic_dismiss"
        android:alpha="0.35"
        android:visibility="gone" />

    <!-- Subtle reset: only shown once a time is recorded, so an accidental
         screen touch can't dismiss the record. Kept faint/borderless so it
         stays out of the way of a celebratory screenshot. -->
    <ImageButton
        android:id="@+id/resetButton"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="center_horizontal|bottom"
        android:layout_marginBottom="48dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/timer_reset"
        android:src="@drawable/ic_reset"
        android:alpha="0.35"
        android:visibility="gone" />

</FrameLayout>
